{
  "name": "ts-flatten",
  "description": "A utility function that takes in a value and returns a flatted version of that value",
  "version": "0.0.1",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "keywords": [
    "flat",
    "flatten",
    "data-structure",
    "util"
  ],
  "scripts": {
    "build": "tsc -d",
    "dev": "tsc --watch --preserveWatchOutput",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "jest": "^28.1.3",
    "prettier": "2.7.1",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
